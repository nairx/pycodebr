# Write a Python program to:
# Take a string input from the user.
# Print it in uppercase, lowercase, and title case.
# Count how many characters are in it (excluding spaces).
# Hint: Use .upper(), .lower(), .title(), and .replace(" ", "").
